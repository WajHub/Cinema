name: Booking Service CI

on:
  workflow_dispatch:
  pull_request:
    branches: [ "main" ]
    paths:
      - 'booking-service/**'
      - '.github/workflows/reusable-ci.yml'
      - '.github/workflows/booking-service.yml'

jobs:
  run-shared-ci:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      service-name: 'Booking Service'
      service-path: 'booking-service'